name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

# Least privilege: the gate only needs to read the repo.
permissions:
  contents: read

jobs:
  # The gate itself lives in gate.yml so release builds run the identical checks.
  gate:
    uses: ./.github/workflows/gate.yml
